[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "softprism"
version = "0.1.0"
description = "SoftPrism robotics library"
requires-python = ">=3.10"